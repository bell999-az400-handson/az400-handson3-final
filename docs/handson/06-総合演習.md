# Lab 6: 総合演習

## 🎯 目的
このLabでは、Lab 1～5で学習した内容を統合し、エンドツーエンドの DevOps パイプラインを構築します。

## ⏱️ 所要時間
約60分（Azure DevOps プロジェクトセットアップ含む）

## 📋 前提条件
- Lab 0～5 のすべての演習が完了していること
- すべての環境とリソースが利用可能な状態

## 🎓 学習内容

### 統合シナリオ
実際の開発プロジェクトを想定し、以下を実装します：

0. **Azure DevOps プロジェクトと GitHub Service Connection のセットアップ**
1. **GitHub ↔ Azure Boards 連携**
2. **完全な CI/CD パイプライン（GitHub → Azure Pipelines）**
3. **パッケージ管理とアーティファクト**
4. **セキュリティとコンプライアンス**
5. **監視とアラート**

## 📝 演習内容

### シナリオ: マイクロサービス アプリケーションの構築

**要件:**
- フロントエンド（React）とバックエンド（.NET Web API）
- Azure Artifacts でパッケージ管理
- Service Principal による自動デプロイ
- Application Insights による監視
- Teams への通知

### Exercise 0: Azure DevOps プロジェクトのセットアップ

> **重要**: この演習は他のすべての演習の前提条件です。GitHub + Azure DevOps の統合に必要です。

#### 0.1 Azure DevOps プロジェクトの作成

1. https://dev.azure.com にアクセス
2. Organization を選択（または新規作成）
3. **New project** をクリック
4. プロジェクト情報を入力：
   - **Project name**: `az400-handson3-final`
   - **Visibility**: Private
   - **Version control**: Git（ただしGitHubを使用）
   - **Work item process**: Agile
5. **Create** をクリック

#### 0.2 GitHub Service Connection の作成

**Azure PipelinesがGitHubリポジトリにアクセスするために必須の設定**

1. Azure DevOps プロジェクト → **Project Settings** （左下の歯車アイコン）
2. **Pipelines** → **Service connections**
3. **New service connection** → **GitHub** を選択
4. 認証方法を選択：
   - **OAuth** （推奨）または **Personal Access Token**
5. GitHubアカウントを認証して接続を許可
6. Service connection name: `GitHub-Connection` （または任意の名前）
7. **Grant access permission to all pipelines** をチェック
8. **Save**

> **ポイント**: この Service Connection により、Azure Pipelines が GitHub リポジトリのコードを取得し、PRイベントを受信できるようになります。

#### 0.3 Azure リソースの確認と VM の準備

**既存のAzureリソースを確認し、VMをデプロイターゲットとして準備**

**Step 1: リソースの確認**

```powershell
# リソースグループの確認
az group show --name rg-az400-handson3 --query "{name:name, location:location}" -o table

# VM の確認
az vm show --resource-group rg-az400-handson3 --name az400-vm --query "{name:name, osType:storageProfile.osDisk.osType, vmSize:hardwareProfile.vmSize, publicIP:'20.89.144.177'}" -o table

# Application Insights の確認
az monitor app-insights component show --app az400-appinsights --resource-group rg-az400-handson3 --query "{name:name, instrumentationKey:instrumentationKey}" -o table
```

**期待される結果:**
- Resource Group: `rg-az400-handson3` (japaneast)
- VM: `az400-vm` (Linux, Standard_B2s, Public IP: 20.89.144.177)
- Application Insights: `az400-appinsights`

**Step 2: VM の HTTP アクセスを許可（NSG ルール追加）**

```powershell
# ポート 80 (HTTP) を許可
az network nsg rule create `
  --resource-group rg-az400-handson3 `
  --nsg-name az400-vmNSG `
  --name allow-http `
  --priority 1001 `
  --destination-port-ranges 80 `
  --access Allow `
  --protocol Tcp `
  --description "Allow HTTP traffic"

# ポート 5000 (.NET Kestrel default) を許可
az network nsg rule create `
  --resource-group rg-az400-handson3 `
  --nsg-name az400-vmNSG `
  --name allow-dotnet `
  --priority 1002 `
  --destination-port-ranges 5000 `
  --access Allow `
  --protocol Tcp `
  --description "Allow .NET application traffic"
```

**Step 3: VM に .NET 8.0 Runtime をインストール**

```powershell
# VM の電源状態を確認
az vm get-instance-view `
  --resource-group rg-az400-handson3 `
  --name az400-vm `
  --query "instanceView.statuses[?starts_with(code, 'PowerState/')].displayStatus" -o tsv

# VM が停止している場合は起動
az vm start --resource-group rg-az400-handson3 --name az400-vm

# VM に SSH 接続（Windows の場合）
ssh azureuser@20.89.144.177

# VM内で以下を実行（Ubuntuの場合）
# Microsoft パッケージリポジトリを追加
wget https://packages.microsoft.com/config/ubuntu/22.04/packages-microsoft-prod.deb -O packages-microsoft-prod.deb
sudo dpkg -i packages-microsoft-prod.deb
rm packages-microsoft-prod.deb

# .NET 8.0 Runtime をインストール
sudo apt-get update
sudo apt-get install -y aspnetcore-runtime-8.0

# インストール確認
dotnet --list-runtimes

# アプリケーション用のディレクトリを作成
sudo mkdir -p /var/www/backend
sudo chown -R azureuser:azureuser /var/www/backend

# SSH接続を終了
exit
```

**Step 4: SSH鍵の確認と準備**

Azure Pipelines からVMにSSH接続するための鍵を確認・準備します。

```powershell
# 既存のSSH鍵を確認
ls $env:USERPROFILE\.ssh\ | Format-Table Name, LastWriteTime

# id_rsa が存在するか確認
if (Test-Path $env:USERPROFILE\.ssh\id_rsa) {
    Write-Host "✅ SSH鍵ペア (id_rsa) が既に存在します" -ForegroundColor Green
    
    # 公開鍵の内容を表示
    Write-Host "`n公開鍵の内容:" -ForegroundColor Cyan
    Get-Content $env:USERPROFILE\.ssh\id_rsa.pub
    
    # VMに登録されているか確認
    Write-Host "`nVMへのSSH接続テスト..." -ForegroundColor Cyan
    ssh azureuser@20.89.144.177 "echo '✅ SSH接続成功'"
    
    if ($LASTEXITCODE -eq 0) {
        Write-Host "✅ 既存の鍵でVMに接続できます。この鍵を使用してください。" -ForegroundColor Green
    } else {
        Write-Host "⚠️ SSH接続失敗。公開鍵をVMに追加する必要があります。" -ForegroundColor Yellow
    }
} else {
    Write-Host "⚠️ SSH鍵が見つかりません。新しく作成します..." -ForegroundColor Yellow
    
    # 新しいSSH鍵ペアを生成
    ssh-keygen -t rsa -b 4096 -f $env:USERPROFILE\.ssh\id_rsa -N '""'
    
    # 公開鍵をVMに追加
    $publicKey = Get-Content $env:USERPROFILE\.ssh\id_rsa.pub -Raw
    az vm user update `
      --resource-group rg-az400-handson3 `
      --name az400-vm `
      --username azureuser `
      --ssh-key-value $publicKey
    
    Write-Host "✅ 新しいSSH鍵を作成し、VMに登録しました" -ForegroundColor Green
}
```

> **🎯 AZ-400試験のポイント**: 
> - SSH鍵認証はパスワード認証より安全
> - 秘密鍵（`id_rsa`）は厳重に管理し、共有しない
> - 公開鍵（`id_rsa.pub`）のみをVMに配置
> - 既存の鍵がある場合は再利用可能

---

**Step 5: Azure Pipelines SSH Service Connection の作成**

1. Azure DevOps → **Project Settings** → **Service connections**
2. **New service connection** → **SSH** を選択
3. 接続情報を入力：
   - **Host name**: `20.89.144.177`
   - **Port**: `22`
   - **Username**: `azureuser` (VMの管理者ユーザー名)
   - **Password or Private key**: **Private key** を選択
   - **Private key**: `C:\Users\<YourUsername>\.ssh\id_rsa` の内容をコピー&ペースト
   - **Service connection name**: `az400-vm-ssh`
4. **Verify connection** をクリックして接続テスト
5. **Save**

> **💡 ヒント**: 秘密鍵の内容を取得するには:
> ```powershell
> Get-Content $env:USERPROFILE\.ssh\id_rsa -Raw | clip  # クリップボードにコピー
> ```
> 
> **⚠️ セキュリティ注意**: 
> - 秘密鍵は機密情報です。誤って共有・公開しないでください
> - Azure DevOps Service Connection に保存された鍵は暗号化されます

### Exercise 1: Work Items とリポジトリのセットアップ

#### 1.1 Work Item の作成

**CSVインポートまたは自動化スクリプトを使用して作成済み**

作成されたWork Items:

1. **Epic #607**: `Microservices Application Development`
   - Description: `フロントエンドとバックエンドのマイクロサービス開発`

2. **Features** (Epic #607 の子として):
   - **#608**: `Backend API Development`
   - **#609**: `Frontend UI Development`
   - **#610**: `CI/CD Pipeline Setup`
   - **#611**: `Monitoring and Alerts`

3. **User Stories** (各 Feature の子として):
   - Backend (#608): **AB#612**: `Create User API endpoint`
   - Backend (#608): **AB#613**: `Implement authentication`
   - Frontend (#609): **AB#614**: `Create login page`
   - Pipeline (#610): **AB#615**: `Setup build pipeline`
   - Monitoring (#611): **AB#616**: `Configure Application Insights`

> **確認**: Azure DevOps → Boards → Work items で階層構造を確認してください。

#### 1.2 GitHub リポジトリの作成

```powershell
# 新しいディレクトリを作成
New-Item -ItemType Directory -Path ~\github\az400-handson3-final
Set-Location ~\github\az400-handson3-final

# Git 初期化
git init
git branch -M main

# README を作成
"# AZ-400 Final Project - Microservices Application" | Out-File README.md -Encoding utf8
git add README.md
git commit -m "Initial commit"
```

**GitHub で新しいリポジトリを作成:**

1. https://github.com/orgs/bell999-az400-handson/repositories にアクセス
2. **New repository** ボタンをクリック
3. **Repository name**: `az400-handson3-final`
4. **Public** または **Private** を選択
5. ❌ **重要**: 「Add a README file」「Add .gitignore」「Choose a license」は**すべてチェックを外す**（空のリポジトリを作成）
6. **Create repository** をクリック

リポジトリ作成後、ローカルからpush:
```powershell
git remote add origin https://github.com/bell999-az400-handson/az400-handson3-final.git
git push -u origin main
```

### Exercise 2: バックエンド API の開発

#### 2.1 .NET Web API の作成

```powershell
# バックエンドフォルダを作成してAPI プロジェクトを作成
mkdir backend
Set-Location backend
dotnet new webapi -n backend
Set-Location backend

# Application Insights を追加
dotnet add package Microsoft.ApplicationInsights.AspNetCore

# Entity Framework Core を追加（データアクセス用）
dotnet add package Microsoft.EntityFrameworkCore.InMemory
```

#### 2.2 User モデルの作成

`backend/Models/User.cs`:
```csharp
namespace backend.Models;

public class User
{
    public int Id { get; set; }
    public string Username { get; set; } = string.Empty;
    public string Email { get; set; } = string.Empty;
    public DateTime CreatedAt { get; set; } = DateTime.UtcNow;
}
```

#### 2.3 DbContext の作成

`backend/Data/UserDbContext.cs`:
```csharp
using Microsoft.EntityFrameworkCore;
using backend.Models;

namespace backend.Data;

public class UserDbContext : DbContext
{
    public UserDbContext(DbContextOptions<UserDbContext> options) : base(options)
    {
    }

    public DbSet<User> Users { get; set; }
}
```

#### 2.4 Users Controller の作成

`backend/Controllers/UsersController.cs`:
```csharp
using Microsoft.ApplicationInsights;
using Microsoft.AspNetCore.Mvc;
using Microsoft.EntityFrameworkCore;
using backend.Data;
using backend.Models;

namespace backend.Controllers;

[ApiController]
[Route("api/[controller]")]
public class UsersController : ControllerBase
{
    private readonly UserDbContext _context;
    private readonly TelemetryClient _telemetry;
    private readonly ILogger<UsersController> _logger;

    public UsersController(UserDbContext context, TelemetryClient telemetry, ILogger<UsersController> logger)
    {
        _context = context;
        _telemetry = telemetry;
        _logger = logger;
    }

    [HttpGet]
    public async Task<ActionResult<IEnumerable<User>>> GetUsers()
    {
        _telemetry.TrackEvent("GetUsers");
        _logger.LogInformation("Getting all users");
        return await _context.Users.ToListAsync();
    }

    [HttpGet("{id}")]
    public async Task<ActionResult<User>> GetUser(int id)
    {
        _telemetry.TrackEvent("GetUser", new Dictionary<string, string> { { "UserId", id.ToString() } });
        var user = await _context.Users.FindAsync(id);

        if (user == null)
        {
            _logger.LogWarning("User {UserId} not found", id);
            return NotFound();
        }

        return user;
    }

    [HttpPost]
    public async Task<ActionResult<User>> CreateUser(User user)
    {
        _telemetry.TrackEvent("CreateUser");
        _context.Users.Add(user);
        await _context.SaveChangesAsync();
        _logger.LogInformation("User {UserId} created", user.Id);

        return CreatedAtAction(nameof(GetUser), new { id = user.Id }, user);
    }
}
```

#### 2.5 Program.cs の更新

`backend/Program.cs`:
```csharp
using Microsoft.EntityFrameworkCore;
using backend.Data;

var builder = WebApplication.CreateBuilder(args);

// Application Insights
builder.Services.AddApplicationInsightsTelemetry();

// DbContext (In-Memory)
builder.Services.AddDbContext<UserDbContext>(options =>
    options.UseInMemoryDatabase("UsersDb"));

// CORS 設定（フロントエンドからのアクセス用）
builder.Services.AddCors(options =>
{
    options.AddPolicy("AllowAll", policy =>
    {
        policy.AllowAnyOrigin()
              .AllowAnyMethod()
              .AllowAnyHeader();
    });
});

builder.Services.AddControllers();
builder.Services.AddEndpointsApiExplorer();
builder.Services.AddSwaggerGen();

var app = builder.Build();

if (app.Environment.IsDevelopment())
{
    app.UseSwagger();
    app.UseSwaggerUI();
}

app.UseCors("AllowAll");
app.UseHttpsRedirection();
app.UseAuthorization();
app.MapControllers();

app.Run();
```

#### 2.6 コミットと Push（AB# を使用）

```powershell
# リポジトリのルートに戻る
Set-Location ../..

# 変更をコミット
git checkout -b feature/user-api
git add .
git commit -m "AB#612: Create User API endpoint"
git push origin feature/user-api
```

### Exercise 3: CI/CD パイプラインの構築

#### 3.1 完全な YAML パイプライン

`azure-pipelines.yml`:
```yaml
# azure-pipelines.yml - Complete CI/CD Pipeline
trigger:
  branches:
    include:
    - main
  paths:
    include:
    - backend/**

pr:
  branches:
    include:
    - main

variables:
  buildConfiguration: 'Release'
  NUGET_PACKAGES: $(Pipeline.Workspace)/.nuget/packages
  # System.Debug: true  # デバッグ時のみ有効化

stages:
# ステージ 1: ビルドとテスト
- stage: Build
  displayName: 'Build and Test'
  jobs:
  - job: BuildJob
    displayName: 'Build API'
    pool:
      vmImage: 'ubuntu-latest'
    steps:
    # .NET のセットアップ
    - task: UseDotNet@2
      displayName: 'Use .NET 8.0'
      inputs:
        version: '8.0.x'
    
    # NuGet キャッシュ
    - task: Cache@2
      displayName: 'Cache NuGet Packages'
      inputs:
        key: 'nuget | "$(Agent.OS)" | **/packages.lock.json'
        restoreKeys: |
          nuget | "$(Agent.OS)"
        path: $(NUGET_PACKAGES)
    
    # 依存関係の復元
    - task: DotNetCoreCLI@2
      displayName: 'Restore Dependencies'
      inputs:
        command: 'restore'
        projects: 'backend/**/*.csproj'
    
    # ビルド
    - task: DotNetCoreCLI@2
      displayName: 'Build Solution'
      inputs:
        command: 'build'
        projects: 'backend/**/*.csproj'
        arguments: '--configuration $(buildConfiguration) --no-restore'
    
    # テスト（テストプロジェクトがある場合）
    - task: DotNetCoreCLI@2
      displayName: 'Run Tests'
      inputs:
        command: 'test'
        projects: 'backend/**/*Tests.csproj'
        arguments: '--configuration $(buildConfiguration) --no-build --collect:"XPlat Code Coverage"'
    
    # コードカバレッジレポート
    - task: PublishCodeCoverageResults@1
      displayName: 'Publish Code Coverage'
      inputs:
        codeCoverageTool: 'Cobertura'
        summaryFileLocation: '$(Agent.TempDirectory)/**/coverage.cobertura.xml'
    
    # 発行
    - task: DotNetCoreCLI@2
      displayName: 'Publish Application'
      inputs:
        command: 'publish'
        publishWebProjects: false
        projects: 'backend/backend.csproj'
        arguments: '--configuration $(buildConfiguration) --output $(Build.ArtifactStagingDirectory)'
        zipAfterPublish: true
    
    # アーティファクトの発行
    - task: PublishBuildArtifacts@1
      displayName: 'Publish Build Artifacts'
      inputs:
        PathtoPublish: '$(Build.ArtifactStagingDirectory)'
        ArtifactName: 'drop'

# ステージ 2: セキュリティスキャン
- stage: Security
  displayName: 'Security Scan'
  dependsOn: Build
  condition: succeeded()
  jobs:
  - job: SecurityScan
    pool:
      vmImage: 'ubuntu-latest'
    steps:
    - script: |
        echo "Running security vulnerability scan..."
        # 実際には OWASP Dependency Check などを実行
        dotnet list backend/backend.csproj package --vulnerable
      displayName: 'Vulnerability Scan'

# ステージ 3: デプロイ（Dev環境）
# 注意: デプロイ前に az400-vm が起動していることを確認してください
# az vm start --resource-group rg-az400-handson3 --name az400-vm
- stage: DeployDev
  displayName: 'Deploy to Dev'
  dependsOn: Security
  condition: and(succeeded(), eq(variables['Build.SourceBranch'], 'refs/heads/main'))
  jobs:
  - deployment: DeployDevJob
    displayName: 'Deploy to Dev Environment'
    pool:
      vmImage: 'ubuntu-latest'
    environment: 'Development'
    strategy:
      runOnce:
        deploy:
          steps:
          - task: DownloadBuildArtifacts@1
            inputs:
              buildType: 'current'
              downloadType: 'single'
              artifactName: 'drop'
              downloadPath: '$(System.ArtifactsDirectory)'
          
          # アーティファクトを解凍
          - task: ExtractFiles@1
            displayName: 'Extract Published Files'
            inputs:
              archiveFilePatterns: '$(System.ArtifactsDirectory)/drop/*.zip'
              destinationFolder: '$(System.ArtifactsDirectory)/extracted'
          
          # SSH経由でVMにファイルをコピー
          - task: CopyFilesOverSSH@0
            displayName: 'Copy Files to VM'
            inputs:
              sshEndpoint: 'az400-vm-ssh'
              sourceFolder: '$(System.ArtifactsDirectory)/extracted'
              contents: '**'
              targetFolder: '/var/www/backend'
              cleanTargetFolder: true
              overwrite: true
          
          # VMでアプリケーションを再起動
          - task: SSH@0
            displayName: 'Restart Application on VM'
            inputs:
              sshEndpoint: 'az400-vm-ssh'
              runOptions: 'inline'
              inline: |
                # アプリケーションを停止（既存のプロセスがあれば）
                sudo pkill -f "dotnet.*backend.dll" || true
                
                # systemd サービスファイルを作成（初回のみ）
                if [ ! -f /etc/systemd/system/backend.service ]; then
                  sudo tee /etc/systemd/system/backend.service > /dev/null <<EOF
                [Unit]
                Description=Backend API Service
                After=network.target

                [Service]
                Type=simple
                WorkingDirectory=/var/www/backend
                ExecStart=/usr/bin/dotnet /var/www/backend/backend.dll
                Restart=always
                RestartSec=10
                User=azureuser
                Environment=ASPNETCORE_ENVIRONMENT=Production
                Environment=ASPNETCORE_URLS=http://0.0.0.0:5000

                [Install]
                WantedBy=multi-user.target
                EOF
                
                  sudo systemctl daemon-reload
                  sudo systemctl enable backend.service
                fi
                
                # Application Insights Connection String を環境変数として設定
                # Managed Identity で Azure CLI にログイン
                az login --identity --allow-no-subscriptions
                
                # Application Insights の接続文字列を取得して systemd override に設定
                CONN_STRING=$(az monitor app-insights component show --app az400-appinsights --resource-group rg-az400-handson3 --query connectionString -o tsv)
                sudo mkdir -p /etc/systemd/system/backend.service.d
                sudo tee /etc/systemd/system/backend.service.d/override.conf > /dev/null <<EOF
                [Service]
                Environment=APPLICATIONINSIGHTS_CONNECTION_STRING=$CONN_STRING
                EOF
                
                sudo systemctl daemon-reload
                
                # サービスを起動
                sudo systemctl restart backend.service
                
                # ステータス確認
                sleep 3
                sudo systemctl status backend.service --no-pager
              readyTimeout: '20000'
```

#### 3.2 Azure Pipeline の作成（GitHub連携）

**Exercise 0.2で作成したService Connectionを使用してパイプラインを作成**

1. Azure DevOps → **Pipelines** → **New pipeline**
2. **Where is your code?** → **GitHub** を選択
3. GitHubアカウントを認証（初回のみ）
4. リポジトリを選択: `bell999-az400-handson/az400-handson3-final`
5. **Existing Azure Pipelines YAML file** を選択
6. Branch: `main`, Path: `/azure-pipelines.yml`
7. **Continue** → **Run** でパイプラインを保存・実行

> **注意**: パイプラインを実行する前に、Exercise 0.3 で作成した SSH Service Connection `az400-vm-ssh` が正しく設定されていることを確認してください。

#### 3.3 GitHub Branch Protection Rules の設定

**GitHubでブランチポリシーを設定（品質ゲート）**

1. GitHub → リポジトリ → **Settings** → **Branches**
2. **Add branch protection rule** をクリック
3. Branch name pattern: `main`
4. 以下のルールを有効化：
   - ✅ **Require a pull request before merging**
     - Require approvals: **1**
   - ✅ **Require status checks to pass before merging**
     - Status checks that are required:
       - Azure Pipelines のビルドチェック（パイプライン実行後に表示されます）
   - ✅ **Require conversation resolution before merging**
   - ✅ **Do not allow bypassing the above settings**
5. **Create** をクリック

> **AZ-400試験のポイント**: 
> - GitHub使用時は**GitHub Branch Protection Rules**でポリシーを設定
> - Azure Repos使用時は**Azure DevOps Branch Policies**で設定
> - Azure BoardsのWork Item連携（AB#）は両方で機能します

### Exercise 4: Pull Request の作成（GitHub）

#### 4.1 GitHub で Pull Request を作成（AB# を使用）

**feature/user-api ブランチから main ブランチへの Pull Request を作成**

1. GitHub → リポジトリ → **Pull requests** タブ
2. **New pull request** をクリック
3. Base branch: `main`, Compare branch: `feature/user-api`
4. **Title**: `AB#612: Create User API endpoint`  
   ⚠️ **重要**: タイトルに `AB#612` を含めることで Azure Boards の Work Item と自動連携
5. **Description**:
   ```markdown
   ## Changes
   - Created User model
   - Implemented Users API controller
   - Added Application Insights telemetry
   - Configured CORS for frontend
   
   ## Related Work Items
   - AB#612
   
   ## Testing
   - Manual testing with Swagger UI
   - API endpoints verified
   ```

6. Reviewers を追加（チームメンバーまたは自分）
7. **Create pull request** をクリック

> **確認ポイント**: 
> - PR作成後、Azure Pipelines が自動的にトリガーされること
> - Azure Boards の Work Item #101 に PR へのリンクが表示されること

#### 4.2 パイプラインの実行と Branch Protection の確認

**GitHub と Azure Pipelines の連携動作を確認**

1. **Azure Pipelines の実行確認**:
   - Azure DevOps → Pipelines → 最新の実行を確認
   - ✅ Build ステージが成功
   - ✅ Security ステージが成功
   - PR用のパイプラインが実行されていること

2. **GitHub PR ページでの確認**:
   - GitHub PR ページに戻る
   - ✅ Status checks: Azure Pipelines ビルドが成功していること
   - ✅ Review required: 承認が必要な状態
   - ✅ Conversations: すべてのコメントが解決済み

3. **Azure Boards での確認**:
   - Azure DevOps → Boards → Work Items → #612
   - ✅ GitHub Pull Request へのリンクが表示されること
   - ✅ Development セクションに PR が関連付けられていること

4. **PR をマージ**:
   - Reviewer が承認
   - すべての checks が通過
   - **Merge pull request** をクリック
   - ブランチ削除（オプション）

### Exercise 5: 監視とアラートの設定

#### 5.1 Application Insights の設定

**既存の Application Insights リソースを確認**

```powershell
# Application Insights の確認（既存リソース）
az monitor app-insights component show `
  --app az400-appinsights `
  --resource-group rg-az400-handson3 `
  --query "{name:name, instrumentationKey:instrumentationKey}" -o table

# Connection String を取得
$connectionString = az monitor app-insights component show `
  --app az400-appinsights `
  --resource-group rg-az400-handson3 `
  --query "connectionString" -o tsv

Write-Host "Connection String: $connectionString"
```

**VM上のアプリケーションに Application Insights を設定**

> **🎯 AZ-400試験のポイント**: シークレット管理のベストプラクティスは **Azure Key Vault + Managed Identity** です。

---

**方法1: Azure Key Vault + Managed Identity（推奨）**

この方法は**AZ-400試験で推奨**されるベストプラクティスです：
- ✅ シークレットをコード/設定ファイルにハードコードしない
- ✅ Managed Identityによる安全なアクセス制御
- ✅ シークレットのローテーションが容易
- ✅ 監査ログが取得可能

```powershell
# Step 1: リソースグループ内のKey Vaultを取得（既存の場合）または作成
$keyVaultName = az keyvault list `
  --resource-group rg-az400-handson3 `
  --query "[0].name" -o tsv

if ([string]::IsNullOrEmpty($keyVaultName)) {
    Write-Host "⚠️ Key Vault が見つかりません。新しく作成します..." -ForegroundColor Yellow
    $keyVaultName = "kv-az400-handson3"  # グローバルに一意な名前に変更してください
    
    az keyvault create `
      --name $keyVaultName `
      --resource-group rg-az400-handson3 `
      --location japaneast `
      --enable-rbac-authorization true
    
    # 新規作成時: 現在のユーザーに Secrets Officer ロールを付与（シークレット作成権限）
    $currentUserId = az ad signed-in-user show --query id -o tsv
    $keyVaultId = az keyvault show --name $keyVaultName --resource-group rg-az400-handson3 --query id -o tsv
    
    az role assignment create `
      --role "Key Vault Secrets Officer" `
      --assignee-object-id $currentUserId `
      --scope $keyVaultId
    
    Write-Host "✅ Key Vault '$keyVaultName' を作成しました（RBAC モード）" -ForegroundColor Green
} else {
    Write-Host "✅ 既存の Key Vault '$keyVaultName' を使用します" -ForegroundColor Green
    
    # 既存 Key Vault 使用時: 現在のユーザーに Secrets Officer ロールを付与（シークレット作成権限）
    $currentUserId = az ad signed-in-user show --query id -o tsv
    $keyVaultId = az keyvault show --name $keyVaultName --resource-group rg-az400-handson3 --query id -o tsv
    
    # ロール割り当て（既に権限がある場合はスキップされる）
    az role assignment create `
      --role "Key Vault Secrets Officer" `
      --assignee-object-id $currentUserId `
      --scope $keyVaultId 2>$null
    
    Write-Host "   シークレット作成権限を確認しました" -ForegroundColor Cyan
}

# Step 2: Application Insights の接続文字列を取得
$connectionString = az monitor app-insights component show `
  --app az400-appinsights `
  --resource-group rg-az400-handson3 `
  --query connectionString -o tsv

Write-Host "Application Insights Connection String: $(if($connectionString){'取得成功 ✅'}else{'取得失敗 ❌'})" -ForegroundColor Cyan

# Step 3: VMにManaged Identityを有効化（System-assigned）
az vm identity assign `
  --name az400-vm `
  --resource-group rg-az400-handson3

# Step 4: Managed Identity の Object ID を取得
$vmIdentityObjectId = az vm show `
  --name az400-vm `
  --resource-group rg-az400-handson3 `
  --query "identity.principalId" -o tsv

Write-Host "VM Managed Identity Object ID: $vmIdentityObjectId"

# Step 5: Application Insights Connection String を Key Vault に保存
az keyvault secret set `
  --vault-name $keyVaultName `
  --name "AppInsights-ConnectionString" `
  --value $connectionString

# Step 6: VMのManaged IdentityにKey Vaultへのアクセス権限を付与
# Key Vault の Resource ID を取得
$keyVaultId = az keyvault show `
  --name $keyVaultName `
  --resource-group rg-az400-handson3 `
  --query id -o tsv

# Azure RBAC でロールを割り当て（AZ-400 推奨）
az role assignment create `
  --role "Key Vault Secrets User" `
  --assignee-object-id $vmIdentityObjectId `
  --assignee-principal-type ServicePrincipal `
  --scope $keyVaultId

Write-Host "✅ Key Vault アクセス権限を付与しました" -ForegroundColor Green

# Step 6.5: VMのManaged IdentityにApplication Insightsへのアクセス権限を付与
# Application Insights への読み取り権限を付与（Monitoring Reader）
az role assignment create `
  --assignee-object-id $vmIdentityObjectId `
  --role "Monitoring Reader" `
  --scope "/subscriptions/$(az account show --query id -o tsv)/resourceGroups/rg-az400-handson3/providers/Microsoft.Insights/components/az400-appinsights"

Write-Host "✅ Application Insights アクセス権限を付与しました" -ForegroundColor Green
```

> **重要**: Step 1-6.5 でKey Vault と Application Insights の準備は完了しました。  
> **Step 7 以降は、Exercise 3 と Exercise 4 でパイプラインデプロイを完了してから実行してください。**  
> （systemd サービス `backend.service` はパイプラインデプロイ時に自動作成されます）

---

### **Step 7: VM内でKey Vaultからシークレットを取得するようにアプリケーションを設定**

> **前提条件**: 
> - ✅ Exercise 3: Azure Pipeline でアプリケーションが VM にデプロイ済み
> - ✅ Exercise 4: Pull Request のマージ完了
> - ✅ systemd サービス `backend.service` が作成済み

```bash
# SSH経由でVMに接続
ssh azureuser@20.89.144.177

# Step 7-1: Azure CLI をVMにインストール（まだない場合）
curl -sL https://aka.ms/InstallAzureCLIDeb | sudo bash

# Step 7-2: Managed Identityでログイン
az login --identity

# Step 7-3: Key Vault名を取得（ローカルで実行した $keyVaultName の値を使用）
# 例: export KEY_VAULT_NAME="kv-az400-handson3-xxxx"
export KEY_VAULT_NAME="kv-az400-handson3"  # ← Step 1 で取得した Key Vault 名に置き換え

# Step 7-4: Key Vault URIを環境変数として設定
sudo mkdir -p /etc/systemd/system/backend.service.d
sudo tee /etc/systemd/system/backend.service.d/override.conf > /dev/null <<EOF
[Service]
Environment="KeyVaultName=$KEY_VAULT_NAME"
EOF

# Step 7-5: サービスを再起動
sudo systemctl daemon-reload
sudo systemctl restart backend.service

# SSH接続を終了
exit
```

> **注意**: この方法を使用する場合、アプリケーションコード（`Program.cs`）でKey Vaultからシークレットを読み取る実装が必要です。詳細は Exercise 5.1-補足 を参照してください。

---

**方法2: 環境変数（systemd service）— 学習用の簡易方法**

本番環境では**非推奨**ですが、学習目的で環境変数を使用する方法：

```powershell
# SSH経由でVMに接続
ssh azureuser@20.89.144.177

# systemd サービスの環境変数を設定
sudo mkdir -p /etc/systemd/system/backend.service.d
sudo tee /etc/systemd/system/backend.service.d/override.conf > /dev/null <<EOF
[Service]
Environment="APPLICATIONINSIGHTS_CONNECTION_STRING=$connectionString"
EOF

# サービスを再起動
sudo systemctl daemon-reload
sudo systemctl restart backend.service

# ステータス確認
sudo systemctl status backend.service

# SSH接続を終了
exit
```

⚠️ **警告**: この方法ではConnection StringがVM上に平文で保存されます。

---

**方法3: appsettings.json（非推奨）**

❌ **AZ-400試験では非推奨** — シークレットをファイルにハードコードする方法：

```powershell
# SSH経由でVMに接続
ssh azureuser@20.89.144.177

# appsettings.json を編集
sudo tee /var/www/backend/appsettings.json > /dev/null <<EOF
{
  "ApplicationInsights": {
    "ConnectionString": "$connectionString"
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  }
}
EOF

# サービスを再起動
sudo systemctl restart backend.service

# SSH接続を終了
exit
```

⚠️ **警告**: 
- ファイルにシークレットをハードコード
- バージョン管理に含めるとシークレット漏洩リスク
- ローテーションが困難

---

**5.1-補足: Key Vault統合のためのコード変更（方法1を使用する場合）**

`backend/Program.cs` に以下を追加：

```csharp
using Azure.Identity;
using Azure.Security.KeyVault.Secrets;

var builder = WebApplication.CreateBuilder(args);

// Key Vault から Connection String を取得
var keyVaultName = Environment.GetEnvironmentVariable("KeyVaultName");
if (!string.IsNullOrEmpty(keyVaultName))
{
    var keyVaultUri = new Uri($"https://{keyVaultName}.vault.azure.net/");
    var client = new SecretClient(keyVaultUri, new DefaultAzureCredential());
    
    try
    {
        KeyVaultSecret secret = client.GetSecret("AppInsights-ConnectionString");
        Environment.SetEnvironmentVariable("APPLICATIONINSIGHTS_CONNECTION_STRING", secret.Value);
    }
    catch (Exception ex)
    {
        Console.WriteLine($"Warning: Failed to retrieve secret from Key Vault: {ex.Message}");
    }
}

// 既存のコード...
builder.Services.AddApplicationInsightsTelemetry();
```

NuGetパッケージを追加：

```powershell
dotnet add backend/backend.csproj package Azure.Identity
dotnet add backend/backend.csproj package Azure.Security.KeyVault.Secrets
```

#### 5.2 アラートルールの作成

```powershell
# VM の CPU 使用率アラート
az monitor metrics alert create `
  --name "VM-HighCPU" `
  --resource-group rg-az400-handson3 `
  --scopes "/subscriptions/fa9bad56-093e-485a-86f9-ca7b251fdf10/resourceGroups/rg-az400-handson3/providers/Microsoft.Compute/virtualMachines/az400-vm" `
  --condition "avg Percentage CPU > 80" `
  --window-size 5m `
  --evaluation-frequency 1m `
  --description "Alert when VM CPU exceeds 80%"

# Application Insights の高いエラー率アラート
az monitor metrics alert create `
  --name "UserAPI-HighErrorRate" `
  --resource-group rg-az400-handson3 `
  --scopes "/subscriptions/fa9bad56-093e-485a-86f9-ca7b251fdf10/resourceGroups/rg-az400-handson3/providers/microsoft.insights/components/az400-appinsights" `
  --condition "count requests/failed > 10" `
  --window-size 5m `
  --evaluation-frequency 1m `
  --description "Alert when API error count exceeds 10 in 5 minutes"
```

### Exercise 6: ダッシュボードの作成

#### 6.1 Azure DevOps ダッシュボード

1. Azure DevOps → Dashboards → New Dashboard
2. Name: `AZ400 Final Project Dashboard`
3. ウィジェットを追加：
   - **Lead Time**: User Story の完了時間
   - **Cycle Time**: 作業時間
   - **Velocity**: スプリント完了数
   - **Query Results**: Active Bugs
   - **Build History**: パイプラインの成功率

#### 6.2 Azure Portal ダッシュボード

**方法1: メトリックをピン留めする（推奨）**

1. **Application Insights のメトリックを追加**
   ```
   Azure Portal → az400-appinsights → Monitoring → Metrics
   
   - Metric 1: Server requests (カウント) → "Pin to dashboard" をクリック
   - Metric 2: Failed requests (カウント) → "Pin to dashboard" をクリック
   - Metric 3: Server response time (平均) → "Pin to dashboard" をクリック
   ```

2. **Virtual Machine のメトリックを追加**
   ```
   Azure Portal → az400-vm → Monitoring → Metrics
   
   - Metric 1: Percentage CPU (平均) → "Pin to dashboard" をクリック
   - Metric 2: Network In Total (合計) → "Pin to dashboard" をクリック
   - Metric 3: Disk Read Bytes (合計) → "Pin to dashboard" をクリック
   ```

3. **ダッシュボードを整理**
   ```
   Azure Portal → Dashboards → Shared dashboards
   → 名前を "AZ400 Monitoring Dashboard" に変更
   → タイルのサイズと配置を調整
   → "Done customizing" → "Save" をクリック
   ```

**方法2: Metrics Chart タイルを使用**

1. Azure Portal → Dashboards → New dashboard
2. Name: `AZ400 Monitoring Dashboard`
3. "Tile Gallery" から **Metrics chart** を追加
4. 各タイルで以下を設定：
   - リソース: az400-appinsights または az400-vm を選択
   - メトリック: Request rate, Failed requests, CPU percentage など
   - 集計: Average, Count, Sum など
   - タイトルを設定

> **AZ-400 ヒント**: 方法1（ピン留め）の方が素早く正確にダッシュボードを作成できます。

### Exercise 7: エンドツーエンドのテスト

#### 7.1 アプリケーションのテスト

**前提条件チェック**

接続エラーが発生する場合、以下を順番に確認してください：

```powershell
# 1. VM が起動しているか確認
az vm show --resource-group rg-az400-handson3 --name az400-vm --query "powerState" -o tsv
# 期待値: "VM running"

# 2. NSG ルールでポート 5000 が開いているか確認
az network nsg rule list --resource-group rg-az400-handson3 --nsg-name az400-vmNSG --query "[?destinationPortRange=='5000'].{Name:name,Priority:priority,Access:access}" -o table
# 期待値: AllowPort5000 ルールが Allowed

# 3. VM内でサービスが起動しているか確認
ssh azureuser@20.89.144.177 "sudo systemctl status backend.service"
# 期待値: "active (running)"

# 4. VM内でポート5000がリッスンしているか確認
ssh azureuser@20.89.144.177 "sudo netstat -tuln | grep :5000"
# 期待値: tcp 0 0.0.0.0:5000 ... LISTEN
```

**API テスト**

```powershell
# API エンドポイントをテスト（VM Public IP）
$apiUrl = "http://20.89.144.177:5000"

# ユーザー一覧を取得
Invoke-RestMethod -Uri "$apiUrl/api/users" -Method Get

# ユーザーを作成

```

#### 7.2 Application Insights でテレメトリを確認

1. Azure Portal → Application Insights → Investigate → Live Metrics
2. リアルタイムでリクエストが表示されることを確認
3. Monitoring → Logs → カスタムイベントを確認:
   ```kql
   customEvents
   | where timestamp > ago(1h)
   | where name == "GetUsers" or name == "CreateUser"
   | project timestamp, name, customDimensions
   | order by timestamp desc
   ```

### Exercise 8: 継続的改善

#### 8.1 パフォーマンスの最適化

Application Insights のデータを活用して、パフォーマンス問題を特定し、継続的に改善します。

##### 📊 1. 遅いクエリを特定する

**Azure Portal での確認**：

1. **Application Insights** → 左メニュー **「調査」**(investigate) → **「パフォーマンス」** を開く
2. **「操作」タブ**で各エンドポイントの応答時間を確認：
   - 平均応答時間
   - 95パーセンタイル値（P95）
   - リクエスト数
3. **遅いエンドポイントをクリック**して詳細を表示：
   - 依存関係の呼び出し時間
   - データベースクエリの実行時間
   - 外部 API 呼び出しの時間

**KQL クエリで詳細分析**：

Application Insights → Monitoring -> **「ログ」** (Logs) で以下のクエリを実行：

```kql
// 平均応答時間が500ms以上のリクエストを特定
requests
| where timestamp > ago(24h)
| summarize 
    RequestCount = count(),
    AvgDuration = avg(duration),
    P95Duration = percentile(duration, 95),
    P99Duration = percentile(duration, 99)
    by name
| where AvgDuration > 500  // 500ms以上
| order by AvgDuration desc
| project 
    Endpoint = name,
    RequestCount,
    AvgDuration_ms = round(AvgDuration, 2),
    P95_ms = round(P95Duration, 2),
    P99_ms = round(P99Duration, 2)
```

##### ⚠️ 2. エラー率の高いエンドポイントを特定する

**Azure Portal での確認**：

1. **Application Insights** → **「調査」**(investigate) → **「エラー」** (Failures) を開く
2. **上位のエラー**を確認：
   - HTTP ステータスコード別（400, 404, 500, 503 など）
   - エンドポイント別のエラー数
   - エラー率（%）
3. **エラーをクリック**して詳細を表示：
   - スタックトレース
   - 例外メッセージ
   - 発生時刻と頻度

**KQL クエリでエラー率を分析**：

```kql
// エラー率が5%以上のエンドポイントを特定
requests
| where timestamp > ago(24h)
| summarize 
    TotalRequests = count(),
    FailedRequests = countif(success == false),
    ErrorRate = round(100.0 * countif(success == false) / count(), 2)
    by name
| where ErrorRate > 5  // 5%以上
| order by ErrorRate desc
| project 
    Endpoint = name,
    TotalRequests,
    FailedRequests,
    ErrorRate_Percent = ErrorRate
```

**特定のエラーの詳細確認**：

```kql
// 500エラーの詳細を確認
requests
| where timestamp > ago(24h)
| where success == false
| where resultCode startswith "5"  // 5xx エラー
| order by timestamp desc
| project 
    timestamp,
    name,
    resultCode,
    duration,
    customDimensions
| take 50
```

##### 📝 3. Work Item を作成して改善する

**Azure Boards で Work Item を作成**：

1. **Azure DevOps** → プロジェクト → **「Boards」** → **「Work Items」**
2. **「+ New Work Item」** → **「Task」** または **「Bug」** を選択
3. **タイトル例**：
   - `"API /api/users の応答時間が平均800msと遅い（目標: 200ms以下）"`
   - `"POST /api/users でバリデーションエラー時に500エラーが発生"`

4. **説明欄に詳細を記載**：

```markdown
## 問題
- エンドポイント: GET /api/users
- 平均応答時間: 800ms
- 目標: 200ms以下

## 原因分析
- Application Insights で確認
- データベースクエリが非効率
- ページネーションなしで全件取得

## 改善案
- [ ] ページネーションの実装
- [ ] データベースインデックスの追加
- [ ] レスポンスキャッシュの導入

## 関連リンク
- Application Insights Performance
- KQL クエリ結果
```

5. **優先度・担当者を設定** → **「Save」** をクリック

##### 🔄 4. 改善サイクルの実行

**例: ページネーション機能の追加（Work Item #617）**

1. **Feature ブランチ作成**：

```powershell
git checkout -b feature/optimize-users-api
```

2. **改善を実装** (backend/Controllers/UsersController.cs)：

```csharp
[HttpGet]
public ActionResult<IEnumerable<User>> GetUsers(
    [FromQuery] int page = 1, 
    [FromQuery] int pageSize = 10)
{
    _telemetryClient.TrackEvent("GetUsers", new Dictionary<string, string>
    {
        { "Page", page.ToString() },
        { "PageSize", pageSize.ToString() }
    });

    var users = _context.Users
        .Skip((page - 1) * pageSize)
        .Take(pageSize)
        .ToList();

    _logger.LogInformation("Retrieved {Count} users (Page {Page}, PageSize {PageSize})", 
        users.Count, page, pageSize);

    return Ok(users);
}
```

3. **コミット＆プッシュ**：

```powershell
git add .
git commit -m "AB#617: Add pagination to Users API to improve performance"
git push -u origin feature/optimize-users-api
```

4. **Pull Request 作成**（AB#617 を本文に含める）
5. **CI/CD パイプライン自動実行** → レビュー → マージ → Dev 環境に自動デプロイ

6. **Application Insights で改善効果を確認**：

```kql
// 改善前後の応答時間を比較
requests
| where name == "GET /Users/GetUsers"
| where timestamp > ago(7d)
| summarize AvgDuration = avg(duration) by bin(timestamp, 1h)
| render timechart
```

##### 📈 5. 継続的な監視

**ダッシュボードでトレンドを監視**：

Azure Portal → **「ダッシュボード」** で以下のメトリックをピン留め：
- **平均応答時間**（目標: <200ms）
- **エラー率**（目標: <1%）
- **リクエスト数**（トラフィック監視）
- **サーバー応答時間（P95）**

**アラートの設定例**：

```powershell
# 応答時間が500ms超えたらアラート
az monitor metrics alert create \
  --name "High-Response-Time-Alert" \
  --resource-group rg-az400-handson3 \
  --scopes "/subscriptions/{subscription-id}/resourceGroups/rg-az400-handson3/providers/microsoft.insights/components/az400-appinsights" \
  --condition "avg requests/duration > 500" \
  --description "API response time exceeded 500ms" \
  --window-size 5m \
  --evaluation-frequency 1m
```

##### 🎯 AZ-400 のポイント

この Exercise 8.1 は **AZ-400 ドメイン 5（監視とフィードバック）** を実践します：

✅ **データドリブンな意思決定**
- Application Insights のメトリクスとログを活用
- KQL でデータを分析し、問題を定量的に特定

✅ **継続的改善のサイクル**
- 監視 → 分析 → 計画（Work Item作成） → 実装 → デプロイ → 再監視

✅ **統合された DevOps ワークフロー**
- Azure Boards（計画） + Git（コード） + Azure Pipelines（CI/CD） + Application Insights（監視）の連携

#### 8.2 新しい Feature の追加

1. Work Item を作成: `AB#106: Add pagination to Users API`
2. Feature ブランチを作成
3. 変更を実装
4. Pull Request を作成（AB#106 を含める）
5. CI/CD パイプラインが自動実行
6. レビュー後にマージ
7. Dev 環境に自動デプロイ

## 📊 総合演習のまとめ

### 実装したDevOpsプラクティス

✅ **1. 計画とトラッキング**
- Work Item による作業管理
- Epic → Feature → User Story の階層
- AB# による GitHub/Azure Repos 連携

✅ **2. ソース管理**
- Git によるバージョン管理
- Branch Policy による品質ゲート
- Pull Request ベースのワークフロー

✅ **3. CI/CD**
- 自動ビルドとテスト
- キャッシュによる高速化
- マルチステージパイプライン
- 環境ごとのデプロイメント

✅ **4. セキュリティ**
- Service Principal による認証
- Key Vault でのシークレット管理
- セキュリティスキャン

✅ **5. 監視とフィードバック**
- Application Insights によるテレメトリ
- アラートと通知
- ダッシュボードによる可視化

### DevOps ライフサイクル

```
Plan (Azure Boards)
    ↓
Code (Git/GitHub/Azure Repos)
    ↓
Build (Azure Pipelines)
    ↓
Test (自動テスト)
    ↓
Release (Azure Web App)
    ↓
Monitor (Application Insights)
    ↓
Feedback (Dashboards/Alerts)
    ↓
← 改善サイクルへ
```

## ✅ 最終確認チェックリスト

### Azure DevOps プロジェクト
- [ ] Azure DevOps プロジェクトが作成されている
- [ ] GitHub Service Connection が設定されている
- [ ] GitHub リポジトリへのアクセスが成功している

### Azure Boards
- [ ] Epic/Feature/User Story が作成されている
- [ ] AB# で Work Item がリンクされている
- [ ] GitHub PR と Work Item が連携している
- [ ] ダッシュボードが作成されている

### ソース管理（GitHub）
- [ ] GitHub リポジトリが設定されている
- [ ] Branch Protection Rules が有効
- [ ] Pull Request が正しく機能する
- [ ] Azure Boards との連携が動作している

### CI/CD パイプライン
- [ ] YAML パイプラインが動作している
- [ ] Cache タスクが機能している
- [ ] アーティファクトが発行されている
- [ ] 自動デプロイが成功している

### セキュリティ
- [ ] Service Principal が設定されている
- [ ] Key Vault でシークレット管理
- [ ] セキュリティスキャンが実行されている

### 監視
- [ ] Application Insights が動作している
- [ ] テレメトリが送信されている
- [ ] アラートが設定されている
- [ ] ダッシュボードが作成されている

## 🎓 AZ-400 試験準備の最終チェック

### 主要トピックの復習

1. **GitHub ↔ Azure Boards**
   - AB# は title と description で認識
   - comment では認識されない

2. **Azure Pipelines**
   - Cache: 依存関係のキャッシュ
   - Artifacts: ビルド成果物
   - System.Debug = true: 詳細ログ

3. **Azure Artifacts**
   - BACPAC: schema + data
   - DACPAC: schema only
   - Views: @Local/@Prerelease/@Release

4. **セキュリティ**
   - Service Principal: AppId + Secret + TenantId
   - PIM: Premium P2 必須

5. **監視**
   - Lead Time: 総時間（MTTR）
   - Cycle Time: 作業時間
   - Velocity: 完了数
   - Service Map: Dependency Agent 必須

## 🏆 お疲れ様でした！

すべてのLabを完了しました！  
これで AZ-400 試験に向けた実践的なスキルが身についています。

### 次のステップ
1. Microsoft Learn のラーニングパスを復習
2. 公式の模擬試験を受験
3. 弱点分野を重点的に学習
4. 実際のプロジェクトで実践

**Good luck with your AZ-400 exam! 🎉**

---

## 📚 参考リンク
- [AZ-400 試験ガイド](https://learn.microsoft.com/certifications/exams/az-400)
- [Azure DevOps ドキュメント](https://learn.microsoft.com/azure/devops/)
- [Azure Well-Architected Framework](https://learn.microsoft.com/azure/well-architected/)
